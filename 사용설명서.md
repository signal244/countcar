# Count Car Ver6.0 통합 사용설명서

이 문서는 로컬 GUI, 로컬 CLI, Google Colab, 분석라인 편집, 카운팅 및 문제 해결 방법을 한곳에 정리한 기준 문서입니다.

## 1. 프로그램 구조와 실행 방식

전체 처리 흐름은 다음과 같습니다.

```text
로컬 GUI ─┐
로컬 CLI ─┼─> DetectionService ─> YOLO 탐지/추적 ─> SQLite(track_trajs)
Colab CLI ─┘                                      └> 병합·가상 이벤트·카운팅 ─> Excel/CSV
```

`src/services/detection_service.py`의 `DetectionService`는 PySide6에 의존하지 않는 공통 계층입니다. GUI의 작업 스레드와 `src.pipeline.detect_to_db` CLI가 모두 이 서비스를 호출합니다.

따라서 공통 계층으로 옮겨도 기존 기능은 유지됩니다.

- 로컬 GUI: 파일 선택, 진행 표시, 중지, 궤적 확인 같은 화면 기능을 담당합니다.
- 로컬 CLI: 화면 없이 배치 작업을 수행합니다.
- Colab: 같은 CLI와 서비스를 GPU 환경에서 실행합니다.
- 달라지는 것은 장치(`cpu`/`cuda`), 파일 경로와 입력 화면뿐이며 탐지·추적·DB 저장 로직은 같습니다.

## 2. 주요 폴더

- `src/app.py`: GUI 진입점
- `src/services/detection_service.py`: 공통 탐지 실행과 세션 관리
- `src/pipeline/detect_track.py`: YOLO 탐지·트래킹
- `src/pipeline/count_tracks.py`: 라인 교차와 시간대별 집계
- `src/pipeline/track_merge.py`: 끊어진 트랙 후보 분석 및 병합
- `src/pipeline/virtual_events.py`: 외삽 기반 가상 교차 이벤트
- `src/pipeline/geometry.py`: 공통 선분 교차와 방향 판정
- `src/config/model_profiles.py`: 모델별 클래스 목록과 차종 설정
- `src/ui/model_selector.py`, `src/ui/class_dialogs.py`: 모델·차종 선택 UI
- `src/services/colab_export.py`: 현재 설정을 Colab용으로 내보내기
- `src/db/queries.py`: DB에 저장된 차종 조회
- `src/db/schema.py`, `src/db/writer.py`: SQLite 스키마와 궤적 저장
- `src/ui/trajectory_viewer2.py`: 궤적 검토와 후처리 GUI
- `config/app_config.json`: 이식 가능한 공용 기본값
- `config/user_state.json`: 현재 PC에서 마지막으로 사용한 경로와 UI 상태
- `config/model_profiles.json`: 모델별 클래스 캐시와 차종 설정
- `config/lines/`: 현장별 분석라인 JSON
- `colab/`: Colab 노트북, 의존성, Colab 설정
- `output/`: DB 스냅샷과 집계 결과
- `tests/`: 핵심 로직 회귀 테스트

`config/user_state.json`은 자동으로 생성되며 Git에서 제외됩니다. Windows 절대경로나 마지막 선택값은 여기에만 보관되므로 `config/app_config.json`과 Colab 설정이 특정 PC 경로로 오염되지 않습니다.

## 3. 로컬 Windows 설치

프로젝트 루트에서 PowerShell을 열고 실행합니다.

```powershell
python -m venv .venv
.venv\Scripts\Activate.ps1
pip install -r requirements.txt
```

YOLO 모델 파일은 기본적으로 `models/best.pt`에 둡니다. 다른 파일을 쓰려면 GUI에서 선택하거나 `config/app_config.json`의 `model_path`를 변경합니다.

기본 분석라인 예시는 `config/lines/line_settings.sample.json`입니다. 실제 분석에서는 반드시 영상에 맞는 현장별 파일을 선택해야 합니다.

## 4. 로컬 GUI 사용법

실행:

```powershell
python -m src.app
```

권장 순서:

1. 입력 영상, YOLO 모델, 분석라인 JSON, 출력 SQLite 경로를 선택합니다.
   모델은 `models/` 폴더를 자동으로 읽어 드롭다운으로 보여줍니다.
2. `탐지 차종 선택`으로 이 모델에서 잡을 차종을 확인합니다.
3. 프로젝트명, 교차로명, 세션명을 확인합니다.
4. 탐지·추적을 시작합니다.
5. 완료 후 궤적 보기에서 DB 세션과 라인 교차 상태를 확인합니다.
6. 원본 궤적으로 집계할지, 트랙 병합과 가상 이벤트를 적용한 후처리 결과로 집계할지 선택합니다.
7. `집계 차종 설정`으로 Excel에 낼 차종 열을 확인합니다.
8. 교차로는 `turn`, 접근로는 `approach` 모드로 Excel을 생성합니다.

중지는 강제 스레드 종료가 아니라 안전한 중지 요청 방식입니다. 현재 프레임 처리가 끝난 뒤 정리하고, 이미 확정되었거나 체크포인트로 저장된 궤적은 DB에 남습니다.

### 원본과 후처리 기준

- `original`: 탐지기가 저장한 원본 궤적만 사용합니다.
- `postprocess`: 끊어진 트랙 병합 결과와 가상 교차 이벤트를 적용합니다.

처음에는 원본 결과를 보관한 채 두 기준을 비교하는 것이 좋습니다. 자동 병합은 거리, 시간차, 진행 방향, ROI, 차종 조건의 영향을 받으므로 현장별 검토가 필요합니다.

### 모델 선택

모델 드롭다운은 목록을 펼칠 때마다 `models/` 폴더를 다시 읽습니다. 새 모델 파일을 폴더에 넣으면 프로그램을 다시 켜지 않아도 목록에 나타납니다. 폴더 밖의 모델은 목록 맨 아래 `직접 선택…`으로 고릅니다.

### 차종 선택

모델마다 인식하는 차종이 다릅니다. 예를 들어 `best.pt`는 차종 7종을 구분하지만, 공식 YOLO 모델(`yolov8m.pt`, `yolo11l.pt`)은 COCO 80클래스 중 차량군만 의미가 있습니다.

기본 집계 열은 모델에 따라 다음과 같습니다.

| 모델 | 기본 Excel 열 |
|---|---|
| `best.pt` 계열 | 승용차 / 소형버스 / 대형버스 / 소형화물 / 중형화물 / 대형화물 (6열) |
| 공식 YOLO 계열 | 승용차 / 버스 / 화물 (3열) — YOLO의 `car` / `bus` / `truck` 을 그대로 반영 |

공식 YOLO 모델의 이륜차(`motorcycle`)와 자전거(`bicycle`)는 **탐지해서 DB에는 저장하지만 기본 집계 열에는 넣지 않습니다.** 필요하면 `집계 차종 설정`에서 목록에 있는 `이륜차` / `자전거`를 골라 열로 추가하면 되고, 이미 DB에 있으므로 재탐지 없이 다시 집계하면 됩니다.

차종은 두 단계에서 각각 설정합니다.

| 버튼 | 위치 | 하는 일 | 되돌리기 |
|---|---|---|---|
| `탐지 차종 선택` | 탐지 설정 | 모델 클래스를 읽어 **탐지해 DB에 저장할** 차종을 고릅니다 | 불가 — 뺀 차종은 DB에 남지 않습니다 |
| `집계 차종 설정` | 카운팅 설정 | DB에 있는 차종을 **Excel 집계 열**로 묶습니다 | 가능 — 몇 번이든 다시 집계할 수 있습니다 |

두 설정 모두 모델 파일명 기준으로 `config/model_profiles.json`에 저장되므로, 같은 모델을 다시 고르면 이전 설정이 그대로 복원됩니다.

`집계 차종 설정`은 모델이 아니라 **DB에 실제로 저장된 차종 이름**을 읽어 보여줍니다. 따라서 예전에 다른 모델로 만든 DB도 그대로 다시 집계할 수 있습니다. 여러 차종을 같은 열 이름으로 지정하면 합산되고, `(제외)`로 두면 집계에서 빠집니다. 열 순서는 목록에서 `위로`/`아래로`로 바꿉니다.

설정하지 않으면 기존처럼 DB에 저장된 이름을 보고 6열(커스텀 모델) 또는 5열(공식 YOLO)을 자동으로 판별합니다.

### Colab 코드 생성

`Colab 코드 생성` 버튼은 현재 GUI 설정을 두 가지로 내보냅니다.

1. `colab/app_config_colab_<교차로명>.json` — 모든 경로를 `/content/drive/MyDrive/...` 형태로 바꾼 설정 파일
2. 그 설정을 읽어 파이프라인을 실행하는 짧은 셀 코드 (클립보드에 자동 복사)

셀 코드를 Colab에 붙여넣고 실행하면 로컬과 같은 설정·같은 차종 기준으로 GPU에서 돌아갑니다. 탐지에 이어 카운팅까지 하려면 셀 아래쪽 주석 블록을 해제합니다.

구글드라이브 밖에 있는 경로(예: `D:ideos.mp4`)는 Colab에서 열 수 없으므로 경고를 띄웁니다. 해당 파일을 드라이브로 옮기고 다시 생성하세요.

## 5. 로컬 CLI 사용법

### 탐지·추적 후 DB 저장

```powershell
python -m src.pipeline.detect_to_db \
  --video "G:\video\sample.mp4" \
  --config config/app_config.json \
  --line-settings config/lines/line_settings.sample.json \
  --session-id "교차로명 오전첨두"
```

선택 옵션:

- `--overwrite-session`: 같은 `session_id`의 원본과 파생 데이터를 지운 뒤 다시 저장
- `--flush-minutes 5|15|30|60`: 활성 트랙 체크포인트 저장 주기 변경
- `--line-settings <파일>`: 설정 파일의 기본 라인 대신 다른 라인 사용
- `--session-id <문자열>`: 세션 ID 직접 지정

같은 세션이 이미 있고 `--overwrite-session`을 쓰지 않으면 `_YYYYMMDD_HHMMSS` 접미사가 붙은 새 세션으로 저장됩니다.

### DB 카운팅과 Excel/CSV 생성

교차로 회전류:

```powershell
python -m src.pipeline.count_tracks \
  --db output/tracks.sqlite \
  --lines config/lines/line_settings.sample.json \
  --session-id "교차로명 오전첨두" \
  --interval-min 15 \
  --mode turn \
  --analysis-basis original \
  --out-xlsx output/result_xls/counts.xlsx
```

접근로:

```powershell
python -m src.pipeline.count_tracks \
  --db output/tracks.sqlite \
  --lines config/lines/line_settings.sample.json \
  --mode approach \
  --analysis-basis postprocess \
  --out-xlsx output/result_xls/approach_counts.xlsx
```

주요 옵션:

- `--interval-min 5|15|30|60`: 집계 시간 간격
- `--mode turn|approach`: 교차로 회전류 또는 접근로 집계
- `--analysis-basis original|postprocess`: 원본 또는 후처리 궤적
- `--session-id`: 한 세션만 선택. 생략하면 세션을 각각 계산한 뒤 합산
- `--reconnect-dist`, `--reconnect-gap`, `--reconnect-passes`: 재연결 조건
- `--extrap-horizon`: 가상 교차 판정을 위한 외삽 길이
- `--out-xlsx`: Excel 경로
- `--out-csv`: CSV 경로. 지정하면 Excel도 함께 생성

## 6. Google Colab 사용법

로컬 PC의 처리 성능이 부족할 때 Colab GPU를 사용합니다. Colab도 GUI와 동일한 `DetectionService`를 사용하므로 결과 구조가 같습니다.

### 준비

1. Colab 런타임 유형을 GPU로 설정합니다.
2. Google Drive에 프로젝트를 `/content/drive/MyDrive/vm/count_car_ver6.0` 경로로 둡니다.
3. `models/best.pt`가 있는지 확인합니다.
4. 실행할 영상에 맞는 `config/lines/*.json`을 고릅니다.
5. `colab/app_config_drive.json`의 `line_settings_path`가 그 파일을 가리키는지 반드시 확인합니다.

노트북에서 프로젝트 루트가 다른 경우 `ROOT` 값을 실제 경로로 수정합니다.

### 노트북 실행

`colab/영상분석_db저장_0.ipynb`부터 `_3.ipynb`까지는 같은 분석 흐름을 복제한 실행용 노트북입니다. 하나만 사용해도 되며, 여러 Colab 런타임에서 서로 다른 영상을 처리할 때 번호를 나누어 사용할 수 있습니다.

노트북의 기본 영상 규칙은 다음과 같습니다.

```text
/MyDrive/video/{프로젝트명}/{교차로명} {세션명}.mp4
```

실행 중에는 다음 방식으로 데이터를 보호합니다.

- 영상과 작업 DB를 Colab 로컬 `/content`로 복사해 처리
- 기존 Drive DB가 있으면 로컬로 복사해 세션을 추가
- 주기적 flush 때 SQLite backup API로 Drive 스냅샷 저장
- 종료 시 최종 DB를 `output/db_snapshots/tracks_{교차로명}.sqlite`에 저장
- 같은 교차로 DB를 여러 런타임이 동시에 수정하지 않도록 Drive 잠금 사용

Google Drive 위의 SQLite 파일을 직접 열어 장시간 쓰는 방식은 느리고 손상 위험이 있으므로 권장하지 않습니다. 같은 교차로 DB 잠금이 잡혀 있으면 다른 런타임이 끝날 때까지 기다리거나 서로 다른 교차로 DB를 사용합니다.

`코랩_카운팅.ipynb`에서 세션 삭제 SQL 셀을 실행할 때는 대상 DB와 `session_id`를 다시 확인하고 먼저 DB를 복사해 두세요.

### Colab CLI 직접 실행

```bash
%cd /content/drive/MyDrive/vm/count_car_ver6.0
!pip install -r colab/requirements_colab.txt
!python -m src.pipeline.detect_to_db \
  --video /content/input_video.mp4 \
  --config colab/app_config_colab.json \
  --line-settings config/lines/line_settings.sample.json \
  --session-id "교차로명 오전첨두"
```

`device`가 `auto`이면 CUDA 사용 가능 여부를 확인해 GPU를 선택합니다.

## 7. 분석라인 설정

분석라인은 2점 직선뿐 아니라 여러 점을 순서대로 연결한 폴리라인으로 저장됩니다.

```json
{
  "id": "line_1",
  "points": [[100, 200], [180, 220], [260, 300]],
  "bound": "north_bound",
  "in_point": [180, 300]
}
```

### 마우스 조작

- 왼쪽 클릭: 임시 점 추가
- 왼쪽 더블클릭: 점이 2개 이상이면 폴리라인 확정
- 오른쪽 클릭: 점이 2개 이상이면 폴리라인 확정
- `Undo`: 마지막 임시 점 하나 취소
- `포인트 초기화`: 저장 전 임시 점만 제거
- `선택 라인 삭제`: 선택한 저장 라인 제거
- `라인 전체 삭제`: 저장 라인 모두 제거
- `Lines 저장` 또는 `저장`: JSON 파일에 반영

궤적 보기 화면에서는 임시 점이 없거나 하나뿐일 때 우클릭 드래그가 화면 이동으로 동작합니다. 임시 점이 두 개 이상이면 우클릭이 라인 확정으로 동작합니다.

### 권장 편집 순서

1. 영상의 첫 프레임이 보이는지 확인합니다.
2. 고유한 라인 ID와 필요한 `bound`를 입력합니다.
3. 차량이 통과하는 경계를 따라 점을 순서대로 찍습니다.
4. 우클릭, 더블클릭 또는 라인 추가 버튼으로 확정합니다.
5. 목록의 점 개수와 방향을 확인합니다.
6. 반드시 저장 버튼을 눌러 JSON에 기록합니다.

### bound와 교차 판정

대표 값은 `north_bound`, `south_bound`, `east_bound`, `west_bound`입니다. 현장 파일은 한글 접근로명을 사용할 수도 있지만, 같은 현장 안에서는 이름을 일관되게 써야 합니다.

폴리라인의 점이 4개라면 세 개의 작은 선분으로 나누어 판정합니다. 차량 궤적이 어느 한 선분과 교차해도 해당 라인을 통과한 것으로 처리됩니다. 짧은 시간과 거리 안에서 같은 라인을 반복해서 흔드는 궤적은 중복 이벤트로 세지 않도록 디바운스됩니다.

주의 사항:

- 같은 위치를 반복해 만든 길이 0 선분은 무시됩니다.
- 최소 두 점이 있어야 저장됩니다.
- 창을 닫기 전에 저장해야 합니다.
- 기존 2점 라인 JSON도 그대로 사용할 수 있습니다.
- `image_width`, `image_height`는 라인을 그린 원본 영상 크기와 맞아야 합니다.
- `bound`가 비어 있으면 기본 교차 수는 만들 수 있지만 접근로 진입·진출 판정이 불완전할 수 있습니다.

## 8. 설정 파일 관리

### 공용 설정

`config/app_config.json`에는 프로젝트 상대경로와 공유 가능한 기본값만 둡니다.

주요 값:

- `model_path`: YOLO 모델
- `db_path`: 탐지 결과 SQLite
- `tracker_config`: BoT-SORT/ByteTrack 설정
- `confidence_threshold`: 최소 탐지 신뢰도
- `allowed_classes`: 사용할 YOLO 클래스 ID
- `target_fps`: 분석 목표 FPS
- `yolo_imgsz`: YOLO 입력 크기
- `line_settings_path`: 기본 분석라인
- `class_mapping_path`: YOLO 클래스와 내부 차종 매핑
- `count_class_mapping`, `count_class_columns`: Excel 집계 차종 열 (GUI가 채웁니다)
- `flush_interval_minutes`: 활성 트랙 체크포인트 간격
- `max_idle_frames`: 트랙 종료 판단 프레임 수

### 모델 프로파일

`config/model_profiles.json`은 모델 파일별 클래스 목록과 차종 설정을 담습니다.

- `classes`: 모델에서 읽은 클래스 번호와 이름 (모델을 다시 열지 않도록 캐시)
- `detect_class_ids`: 탐지에 사용할 클래스 번호 → `allowed_classes`로 전달
- `excel_mapping`, `excel_columns`: Excel 집계 열 매핑과 순서

`fingerprint`(파일 크기·수정시각)가 달라지면 모델이 교체된 것으로 보고 클래스를 다시 읽습니다. 이때 사용자가 고른 값은 최대한 유지하고, 사라진 클래스만 정리합니다.

`allowed_classes`는 클래스 **번호**라 모델마다 의미가 다릅니다. 예를 들어 번호 4는 `best.pt`에서는 대형화물이지만 COCO 모델에서는 비행기입니다. 이 때문에 모델을 바꿀 때는 반드시 `탐지 차종 선택`을 다시 확인하세요.

탐지 기본값은 집계 기본 열보다 넓게 잡습니다. 탐지에서 뺀 차종은 DB에 남지 않아 되돌릴 수 없지만, 집계는 몇 번이든 다시 할 수 있기 때문입니다.

### 로컬 상태

GUI가 마지막으로 선택한 절대경로, 창 크기, 뷰어 옵션은 `config/user_state.json`에 저장됩니다. 이 파일은 현재 PC 전용이며 Git에 포함되지 않습니다.

### Colab 설정

- `colab/app_config_drive.json`: 제공 노트북의 기준 설정
- `colab/app_config_colab.json`: Colab CLI 실행용 설정

두 파일은 Windows 절대경로를 포함하지 않습니다. 현장별 작업 전에는 모델과 분석라인 경로만 확인하고, DB와 세션 경로는 노트북이 런타임 설정으로 덮어씁니다.

## 9. DB와 결과 파일

주요 테이블:

- `track_trajs`: 트랙 하나당 압축 궤적 한 행
- `tracks`: 이전 형식과의 호환을 위한 프레임 단위 테이블
- `track_merge_map_auto`, `track_merge_map_manual`: 자동·수동 병합
- `track_virtual_events`: 가상 교차 이벤트
- `track_line_events`, `track_line_summary`: 라인 교차 원본과 요약

활성 트랙은 flush 시 체크포인트로 저장되고 정상 종료 시 최종 행으로 교체됩니다. 차종은 한 프레임의 최고 신뢰도만 쓰지 않고 트랙 전체 프레임의 누적 근거를 사용해 결정합니다.

기본 결과 위치:

- DB: `output/tracks.sqlite` 또는 `output/db_snapshots/`
- Excel/CSV: `output/result_xls/`

`output/`과 `models/`는 큰 파일과 현장 자료가 들어갈 수 있으므로 임의로 삭제하거나 Git에 추가하지 마세요.

## 10. 문제 해결

### 모델 파일을 찾지 못함

`models/best.pt`가 실제로 있는지 확인하고, GUI 또는 설정의 `model_path`를 수정합니다. 상대경로는 프로젝트 루트를 기준으로 해석됩니다.

### 분석라인 경고 또는 카운트가 0

- 선택한 JSON이 실제 영상의 현장 파일인지 확인합니다.
- `image_width`와 `image_height`가 원본 영상 해상도와 맞는지 확인합니다.
- 궤적 보기에서 차량 궤적이 라인을 실제로 가로지르는지 확인합니다.
- 접근로 집계라면 `bound`와 `in_point` 방향을 확인합니다.
- 여러 세션이 있는 DB라면 올바른 `session_id`를 선택합니다.

### DB locked 또는 Colab 잠금 대기

같은 SQLite 또는 같은 교차로 Drive DB를 두 작업이 동시에 쓰지 않게 합니다. Colab 잠금은 정상 종료 시 해제됩니다. 런타임이 비정상 종료된 경우 노트북의 stale 잠금 처리 시간이 지난 뒤 다시 시도합니다.

### GUI가 느림

탐지 자체는 GUI 유무보다 GPU 성능의 영향을 크게 받습니다. 로컬에서는 라인 설정과 결과 검토만 하고, 긴 영상 탐지는 Colab GPU 노트북 또는 CLI로 실행하세요.

### Excel 저장 실패

Excel에서 같은 파일을 열어 둔 경우 새 파일명으로 저장될 수 있습니다. 계속 실패하면 파일을 닫고 출력 폴더 쓰기 권한을 확인합니다.

## 11. 개발 검증

핵심 설정, 선분 교차, 중복 교차 억제, 트랙 차종 안정화, DB 체크포인트를 검증하려면 실행합니다.

```powershell
python -m unittest discover -s tests -v
```

전체 영상을 다시 분석하기 전에는 짧은 테스트 영상으로 DB 생성, 궤적 보기, 원본·후처리 Excel 생성까지 한 번 확인하는 것이 좋습니다.
