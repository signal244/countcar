# 프로젝트 검토 — 2026-09-14

## 수정 반영 결과

사용자의 권장 순서 진행 요청에 따라 아래 초기 검토의 1–5번을 수정했다. 아래의 재현 설명과 코드 줄 번호는 수정 전 상태의 기록이다.

- 구형/신형 궤적 읽기를 `src/db/trajectories.py`로 분리하고 집계·GUI 궤적·차종·시간대 조회에 적용했다. 혼합 DB를 세션별로 집계하고 같은 궤적의 구형 복사본은 중복 집계하지 않는다.
- 빈 프레임에서도 유휴 트랙 종료와 주기 저장을 처리한다. 디코딩·추론 오류 시 활성 궤적 저장을 시도하고 스트림·캡처·DB 연결을 정리한다. 정리 실패가 최초 오류를 가리지 않도록 했다.
- DB 쓰기 실패는 트랜잭션을 롤백하여 체크포인트를 보존한다. 읽을 수 있는 프레임이 전혀 없는 영상은 성공으로 처리하지 않는다.
- 덮어쓰기는 별도 부분 세션에 기록한 뒤 성공 시 짧은 트랜잭션으로 교체한다. 오류·취소·교체 실패 시 기존 세션과 파생 결과를 보존한다. 부분 결과 ID는 로그와 중단 결과에 남긴다.
- NaN/Infinity/bool·소수 클래스 ID·불완전한 크기·비유한 좌표를 검증한다. 기존 현장 라인 파일과 공용/Colab 설정은 모두 검증을 통과했다.
- 설명서의 Colab 파일명·PowerShell 줄 연결·덮어쓰기 설명을 갱신했다. Windows Python 3.10용 주요 패키지 버전을 `constraints-windows-py310.txt`에 기록하고 Colab 집계에 필요한 pandas/openpyxl 의존성을 명시했다.
- `AGENTS.md`에 공통 조회 모듈과 추가 회귀 테스트 경로를 반영했다. 대규모 Excel 출력·UI 재구성까지 확대하지 않고, 이번 호환성 수정에 필요한 DB 읽기 책임을 먼저 분리했다.

검증: Python 3.10.8, `QT_QPA_PLATFORM=offscreen`에서 전체 **82개 테스트 통과**(기존 53개 + 신규 29개). 임시 SQLite에서 초기화 전후 조회, 혼합/이름 없는 세션, GUI 로더, 중단·오류·덮어쓰기 롤백, Excel 파일 생성과 입력 경계값을 확인했다.

제한: 실제 모델/영상 추론, Colab GPU 실행, constraints를 이용한 신규 환경 설치는 수행하지 않았다. 설치된 패키지·실제 분석 DB·모델은 변경하지 않았다. 부분 세션도 전체 집계에 포함될 수 있으므로 원본과 부분 결과 비교 시 세션을 명시적으로 선택해야 한다. 커밋·push는 하지 않았다.

## 초기 검토 기록

## 검토 범위와 결과

`CLAUDE.md`, `.github/copilot-instructions.md`, `README.md`, `사용설명서.md`를 먼저 읽고 공통 탐지 서비스, 추적 루프, SQLite 저장·스키마, 카운팅, 설정 검증, GUI 워커·종료 경로와 테스트를 검토했다. 모든 UI 동작 및 Colab 노트북 실행을 검증한 것은 아니다.

- 기존 Python 3.10.8 환경에서 `QT_QPA_PLATFORM=offscreen`으로 `python -m unittest discover -s tests -t . -v` 실행: **53개 통과**.
- 임시 SQLite 및 모의 탐지 스트림으로 아래 결함을 별도 재현했다. 실제 모델 추론·영상 분석은 하지 않았다.
- 공통 `DetectionService` 경유, 압축 궤적 저장, 누적 차종 근거, 후처리 되돌리기 회귀 테스트는 좋은 기반이다.
- 이번 변경은 `AGENTS.md`와 이 검토 문서 추가뿐이다. 제품 코드·설정·패키지·실제 DB는 변경하지 않았다.

## 우선 수정할 사항

### 1. P1 — 빈 신형 테이블이 생기면 구형 궤적을 읽지 못함 [재현]

위치: `src/pipeline/count_tracks.py:245`, `:260`, `:292`, `:1248` 부근.

`load_traj_table()`과 `_has_track_trajs()`는 `track_trajs` 테이블의 존재만 확인한다. `init_db()`는 구형 DB에도 빈 `track_trajs`를 생성하므로, 이 작업 이후 `tracks`의 실제 데이터가 있어도 신형 경로를 선택한다. 뷰어와 병합 관련 코드에도 `init_db()` 호출이 있다.

임시 DB의 구형 `tracks`에 한 행을 넣고 같은 세션을 조회한 결과:

```text
legacy rows before init_db: 1
legacy rows after init_db: 0
```

원본 행이 삭제된 것은 아니지만 화면·집계에서 누락된다. 세션별 데이터 존재 여부로 저장 형식을 선택하거나 명시적 마이그레이션이 필요하다. 구형/신형 세션이 섞인 DB도 처리하고, 같은 궤적이 양쪽에 있는 경우 이중 집계를 막아야 한다.

추가할 회귀 테스트: 구형 DB를 초기화한 뒤 동일 조회·집계 결과 유지, 서로 다른 형식의 세션을 한 DB에서 선택/전체 집계.

### 2. P1 — 탐지 결과가 없는 프레임에서 비활성 트랙 종료·주기 저장을 건너뜀 [재현]

위치: `src/pipeline/detect_track.py:213`, `:317`, `:339` 부근.

`boxes is None` 또는 `boxes.id is None`이면 바로 `continue`해 `_finalize_inactive()`와 체크포인트/flush를 실행하지 않는다. 차량이 사라진 뒤 빈 영상이 이어지면 설정한 유휴 시간·저장 간격이 지나도 트랙이 메모리에 남는다.

1 FPS, 유휴 한도 2프레임, 저장 간격 60초에서 첫 프레임만 차량이 있고 뒤의 65프레임이 빈 모의 스트림 결과:

```text
66 frames, inactive checks: 1 checkpoints: 0 flush: 0
```

정상 EOF에서는 마지막 정리가 실행되지만, 빈 구간 동안에는 주기 저장 보장이 깨진다. 탐지 결과 처리는 조건부로 하되 프레임별 종료·체크포인트 처리는 항상 실행하도록 바꾸는 것이 좋다.

추가할 회귀 테스트: 차량 등장 후 긴 빈 구간, `boxes.id=None` 구간, 빈 구간 중 저장 경계 통과.

### 3. P1 — 추론·디코딩 예외 때 활성 궤적을 저장하지 못함 [재현]

위치: `src/pipeline/detect_track.py:199`, `:342`; `src/db/writer.py`의 writer 종료 처리.

남은 활성 트랙의 최종 저장이 스트림 루프 뒤에만 있다. 스트림 반복 중 예외가 나면 그 부분에 도달하지 못한다. writer의 `close()`는 이미 전달된 버퍼만 저장하므로 추적기 내부 `traj_buf`의 활성 궤적은 복구하지 못한다.

차량 결과 한 프레임 뒤 디코더 예외를 발생시킨 모의 스트림에서 `writer.add_track` 호출은 0회였다. 이전 체크포인트가 있으면 그 시점까지는 남지만 이후 활성 궤적은 유실될 수 있다.

오류 경로에서도 가능한 활성 궤적을 저장하고 스트림 자원을 정리하도록 `try/finally` 또는 별도 종료 경로가 필요하다. DB 자체가 실패한 경우에는 저장을 보장할 수 없으므로 최초 예외를 보존하고 저장 실패도 함께 기록해야 한다.

추가할 회귀 테스트: 스트림 예외, 사용자 중단, 정상 EOF, 정리 중 writer 오류.

### 4. P1 — 덮어쓰기 실행 실패 시 이전 세션을 잃을 수 있음 [정적 검토]

위치: `src/services/detection_service.py:290`–`:293`, `:310` 이후.

덮어쓰기 옵션은 기존 세션과 파생 데이터를 삭제하고 커밋한 후 추적을 시작한다. 모델 생성과 파일 존재 검사는 앞서 수행하지만, 실제 영상 디코딩이나 추론은 뒤에서 실패할 수 있다. 이 경우 이전 정상 결과는 이미 지워진 상태다.

임시 세션/작업 DB에 먼저 분석하고 성공한 뒤 원자적으로 교체하는 방법이 좋다. 중단된 부분 결과의 보관 정책도 정해야 한다. 실행 전체 동안 긴 DB 트랜잭션을 유지하는 방식은 잠금 문제가 있어 피하는 편이 좋다.

추가할 회귀 테스트: 기존 세션 보유 상태에서 첫 프레임 실패·중도 실패·취소·성공 시 각각 원본 보존 또는 교체 확인.

### 5. P2 — 숫자 검증이 비유한 값과 소수 클래스 ID를 허용함 [재현]

위치: `src/config/validation.py:25`, `:64`, `:71` 부근.

범위 비교만으로는 NaN을 걸러내지 못한다. `int(value)` 변환 가능 여부로 클래스 ID를 검사하면 `1.9`도 정수로 간주되어 이후 1로 바뀐다. 좌표 검증도 숫자 변환 가능 여부만 확인한다.

`confidence_threshold=float('nan')`, `allowed_classes=[1.9]`를 전달했을 때 오류 목록은 `[]`였다. 추론 실패나 의도하지 않은 차종 선택을 일으킬 수 있다.

`math.isfinite()`와 정수성·음수·bool 여부를 검증하고, 크기·프레임 수처럼 정수가 필요한 필드의 정책도 명확히 해야 한다. 경계값 중심 테스트를 추가한다.

## 문서·유지보수 개선

- `사용설명서.md:201`, `:207`, `:332`는 현재 없는 `colab/app_config_drive.json`, `영상분석_db저장_0.ipynb`~`_3.ipynb`를 안내한다. 실제 제공 파일은 `colab/app_config_colab.json`, `colab/영상분석_db저장.ipynb` 등이다. 노트북 내부 경로와 대조해 문서를 정리해야 한다.
- README/설명서의 `python -m venv` 안내에는 Python 버전 조건이 없다. `CLAUDE.md`의 기존 환경 안내와 구분하여 신규 설치 버전·검증된 의존성 조합을 명시하면 설치 실패를 줄일 수 있다. 이번에는 패키지를 바꾸지 않았다.
- `requirements.txt`가 하한 버전만 지정하므로 재설치 때 실행 조합이 달라질 수 있다. 로컬/Colab 각각 검증된 constraints를 관리하되 공유 v5 환경을 바로 갱신하지 않는다.
- `src/pipeline/count_tracks.py`의 DB 조회·교차 계산·후처리·Excel 출력 책임을 순차 분리하면 수정 범위와 필요한 코드 읽기량을 줄일 수 있다. 위 결함의 회귀 테스트를 먼저 확보한 뒤 진행한다.

## AGENTS.md와 토큰 사용

루트에 대문자 `AGENTS.md`를 추가했다. 실행 환경, 작업별 시작 파일·테스트, 보존해야 할 규칙, 관련 부분만 읽는 탐색 원칙을 담았다. 긴 인수인계 문서와 이번 결함 목록은 자동 지침에 복제하지 않았다.

공식 문서에 따르면 Codex는 작업 시작 때 `AGENTS.md`를 읽고, 상위부터 현재 작업 디렉터리까지 지침을 결합한다. 별도 fallback 설정 없는 `CLAUDE.md`의 자동 로딩을 전제로 하면 안 된다.
출처: https://learn.chatgpt.com/docs/agent-configuration/agents-md

**예상 효과:** 매 작업마다 환경·진입점·검증 명령을 다시 찾는 탐색과 잘못된 환경 실행을 줄일 수 있다. 다만 지침 본문 자체도 입력이므로 길게 만들수록 비용이 늘며, 실제 절감률은 이번 검토에서 측정하지 않았다.

권장 운영:

1. `AGENTS.md`에는 안정적인 규칙·경로만 유지한다. 작업 로그, 전체 인수인계, 테스트 출력은 넣지 않는다.
2. `CLAUDE.md`와 Copilot 지침의 공통 내용은 향후 공통 원칙을 기준으로 정리하여 중복·불일치를 줄인다. 이번에는 기존 도구 지침을 변경하지 않았다.
3. 같은 규모의 작업에서 입력·출력 토큰, 파일 읽기 횟수, 전체 소스 출력량을 비교해 효과를 측정한다. 절감률을 미리 단정하지 않는다.
4. 새 세션에서 적용된 지침 파일 목록을 확인한다. 이 세션에서는 파일 작성·내용 검증까지 완료했다.

수정 순서는 데이터 보존·조회 문제(1–4), 입력 검증(5), 문서와 모듈 분리를 권한다.
